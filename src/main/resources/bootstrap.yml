spring:
  application:
    name: test-one-service
  profiles:
    active: dev
  cloud:
    config:
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 2
        max-attempts: 10
      uri: http://localhost:8001
      username: config
      password: config
      fail-fast: true
      profile: dev
      label: dev

